{
  "name": "consultoriaambiental",
  "version": "1.0.0",
  "description": "Consultoría Ambiental - Sitio Web",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i public/css/app.css -o public/css/tailwind.css --minify",
    "watch:css": "npx @tailwindcss/cli -i public/css/app.css -o public/css/tailwind.css --watch",
    "build": "npm run build:css"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
